{/*
  Canonical Summary structure (concise, core-first). Body only — frontmatter is generated
  from metadata.yaml by `summarize_paper.py --format mdx --prepare`. Components are
  provided globally by the site (no imports). See config/summary_components.md for the
  full vocabulary. Rules: concise + core-first; keep depth collapsed in <Pass>; be
  self-contained (define non-universal terms with <Term>); declarative English; verify
  numbers against extraction.txt; "Not reported"/"Unclear" for gaps. Motivation ≠
  Contribution — keep them distinct.
*/}

<TLDR>One sentence: the paper's core contribution.</TLDR>

<WhyItMatters>1–2 lines: relevance to my interests / current trends.</WhyItMatters>

## Motivation
The problem and the gap in prior work that makes it worth solving. Problem only — not the solution.

## Contribution (what's new)
The genuine delta vs the closest prior work, and the **mechanistic why** the approach must
take this form (derived from why the baseline fails). Not "prior work couldn't do X so they
added a module." Use <Claim>…</Claim> for assertions and keep it to 2–4 lines.

## Research questions
The core question(s) the work answers. {/* adaptive: for system/benchmark papers with no
explicit RQ, state the central question the work addresses. */}

## Methodology
The approach in your own words. Keep the surface short; tuck internals in a Pass:

<Pass title="Method details">
Architecture / algorithm / dataset or benchmark construction details. Use <Stepper> to walk
a procedure, <MathBlock> for key equations, <Figure src="…" /> for a diagram.
</Pass>

## Main result
The headline result(s) in context: <Evidence src="Tab.X, p.Y">the number vs baseline/human</Evidence>.
Flag <Weak>single-run / no-variance / abstract-vs-table mismatch</Weak> where relevant.

<Pass title="Full results">
  <ResultsTable
    columns={["Setting", "Metric", "Score", "Baseline"]}
    rows={[["…","…","…","…"]]} />
</Pass>

## Implications
Why it matters / what it changes for the field or for my work.

## Limitations
Author-stated and inferred. Use <Limit>…</Limit> for the load-bearing ones.

<Pass title="Evidence check (claim → support)">
  <ClaimEvidence rows={[
    { claim: "…", evidence: "… (Tab.X)", verdict: "holds | partial | unclear" }
  ]} />
Separate experiments that *verify the novelty* from those that *merely confirm the motivation*.
</Pass>

---

## Critiques and Questions
1. Your skeptical read / open questions / what you'd test next.

## Glossary
Define non-universal terms inline where first used, e.g.
<Term def="apply a model patch and run the repo's tests; pass = resolved">execution-based eval</Term>.
List anything that still needs defining here.

{/* NOTES_START */}
### Personal notes
Free-form notes for later.
{/* NOTES_END */}
