{/*
  Canonical Summary structure (concise, core-first). Body only — frontmatter is generated
  from metadata.yaml by `summarize_paper.py --format mdx --prepare`. Components are
  provided globally by the site (no imports). See config/summary_components.md.

  TWO HARD RULES:
  1. CONCISE, BUT KEEP THE KEY POINTS. Capture the paper's important findings/analysis —
     not just the headline number. (e.g. for SWE-bench: "difficulty correlates with
     context length", "finetuned models are sensitive to context-distribution shift".)
     Put the headline up top; collect the rest under "## Key findings". Depth in <Pass>.
  2. USE THE SEMANTIC HIGHLIGHTS for their MEANING (colors are consistent across summaries):
       <Problem>   red    — the problem this paper tackles / a prior-work limitation
       <Novelty>   green  — a novel module/approach/insight this paper introduces
       <Finding src="Tab.X">  teal — a finding / claim / result of THIS paper
       <FollowUp>  blue   — a notion/thread the reader should follow up on
       <Caveat>    orange — a limitation of THIS paper
       <Related>   violet — related work / lineage
       <Term def="…">  glossary chip with hover definition (self-contained)
  Declarative English; specific numbers + anchors; "Not reported"/"Unclear" for gaps;
  verify numbers against extraction.txt. Keep Motivation (<Problem>) ≠ Contribution (<Novelty>).
*/}

<TLDR>One sentence: the paper's core contribution.</TLDR>

<WhyItMatters>1–2 lines: relevance to my interests / current trends.</WhyItMatters>

<KeyStats items={[
  { value: "NN%", label: "the headline metric", sub: "model / setting" },
  { value: "N", label: "scale (tasks/params)", sub: "context" },
]} />

## Motivation
The <Problem>problem this paper tackles</Problem> and the <Problem>gap in prior work</Problem> that makes it worth solving. Problem only — not the solution.

## Contribution (what's new)
The genuine delta. State the <Novelty>insight/mechanism that survives deleting "we propose"</Novelty>, and **why the design must take this form** (derived from why the baseline fails). 2–4 lines.

## Research questions
The core question(s) the work answers. {/* adaptive: for system/benchmark papers, the central question addressed. */}

## Methodology
The approach in your own words; keep the surface short.

<Pass title="Method details">
Architecture / algorithm / dataset or benchmark construction. Use <Stepper>, <MathBlock>, <Figure>.
</Pass>

## Main result
The headline result in context: <Finding src="Tab.X, p.Y">number vs baseline/human</Finding>. For multi-row comparisons use the interactive sortable table:

<SortableTable columns={["Method", "Metric", "Score"]} rows={[["…", "…", 0]]} />

## Key findings
The paper's important findings/analysis beyond the headline — this is where most summaries
fail by omission. 3–6 bullets, each a real result with an anchor:
- <Finding src="§5">a key finding stated plainly</Finding>: one line of explanation.
- <Finding src="Fig.X">another finding</Finding>: …

## Implications
Why it matters / what it changes. Name <Related>related work / the lineage it enables</Related>; flag <FollowUp>threads worth chasing next</FollowUp>.

## Limitations
Author-stated and inferred, as <Caveat>this paper's limitations</Caveat>.

## Critiques and Questions
1. Your skeptical read / open questions.

<SelfCheck q="A comprehension question about the core mechanism or a key finding" a="The answer, grounded in the paper." />

## Glossary
Define non-universal terms inline with <Term def="plain definition">the term</Term>; list any remaining here.

{/* NOTES_START */}
### Personal notes
Free-form notes for later.
{/* NOTES_END */}
