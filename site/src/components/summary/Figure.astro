---
interface Props { src: string; caption?: string; alt?: string; }
const { src, caption, alt } = Astro.props;
---
<figure class="fig">
  <img src={src} alt={alt ?? caption ?? "figure"} loading="lazy" />
  {caption && <figcaption>{caption}</figcaption>}
</figure>
